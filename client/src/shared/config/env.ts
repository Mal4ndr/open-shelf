/**
 * Даний файл є представником папки config (конфігураційні файли), що може зберігати файли
 * змінних оточення, глобальних констант. У випадку поточного файлу зберігається
 * експортна змінна оточення, що може використовуватися у всьому проєкті.
 */
export const env = {
   API_URL: process.env.NEXT_PUBLIC_API_URL!,
};
